# Prometheus 알림 규칙 예시 (실제로는 발동하지 않음)
# 필요에 따라 규칙을 추가하세요.
groups:
  - name: example
    rules:
      - alert: ExampleAlert
        expr: vector(0)
        for: 1m
        labels:
          severity: warning
        annotations:
          summary: "예시 알림 (발동하지 않음)"
